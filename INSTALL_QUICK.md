# 🚀 Быстрая установка Telegram Mini App для шин

## 📋 Что нужно перед установкой?

1. **Сервер:** Ubuntu 20.04+ или Debian 10+
2. **Ресурсы:** минимум 2GB RAM, 10GB диск
3. **Доступ:** root права (sudo)
4. **Порты:** 80, 443 открыты (для HTTPS)

---

## ⚡ Установка за 2 команды

### Вариант 1: С вашим доменом и SSL (РЕКОМЕНДУЕТСЯ для Telegram)

```bash
wget https://raw.githubusercontent.com/wrx861/tyres/main/install.sh
sudo bash install.sh -d ВАШ_ДОМЕН.com -e ВАШ_EMAIL@example.com
```

**Замените:**
- `ВАШ_ДОМЕН.com` → ваш реальный домен (например: `tires.myshop.ru`)
- `ВАШ_EMAIL@example.com` → ваш email

**Требования:**
- ✅ Домен уже указывает на IP вашего сервера (проверьте через `ping`)
- ✅ DNS записи обновились (может занять до 24 часов)

---

### Вариант 2: Локальная разработка (без домена)

```bash
wget https://raw.githubusercontent.com/wrx861/tyres/main/install.sh
sudo bash install.sh
# Нажмите Enter когда спросит про домен
```

---

## 🔧 После установки

### 1. Проверьте учетные данные

Если вы ввели данные во время установки - всё готово! ✅

Для проверки или изменения откройте файл:
```bash
sudo nano /opt/tyres-app/backend/.env
```

Убедитесь что заполнены:
```env
FOURTHCHKI_LOGIN=ваш_логин_4tochki
FOURTHCHKI_PASSWORD=ваш_пароль_4tochki
TELEGRAM_BOT_TOKEN=токен_от_BotFather
ADMIN_TELEGRAM_ID=ваш_telegram_id
```

### 2. Перезапустите приложение

```bash
sudo supervisorctl restart all
```

### 3. Проверьте что работает

```bash
sudo supervisorctl status
```

Все должно быть **RUNNING** ✅

---

## 📱 Где взять учетные данные?

### API 4tochki
- Зарегистрируйтесь на [b2b.4tochki.ru](https://b2b.4tochki.ru)
- Получите логин и пароль

### Telegram Bot Token
1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (выглядит как `123456:ABC-DEF...`)

### Ваш Telegram ID
1. Напишите [@userinfobot](https://t.me/userinfobot)
2. Бот пришлет ваш ID (число, например: `508352361`)

---

## 🌐 Настройка Telegram Mini App

После установки с доменом и SSL:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp`
3. Выберите вашего бота
4. **URL приложения:** `https://ваш-домен.com`

---

## ❓ Частые вопросы

### В какой момент указывать домен?

**НЕПРАВИЛЬНО:** ❌
```bash
wget -qO- https://... | sudo bash  # НЕ БУДЕТ РАБОТАТЬ!
```

**ПРАВИЛЬНО:** ✅
```bash
# Сначала скачиваем
wget https://raw.githubusercontent.com/wrx861/tyres/main/install.sh

# Потом запускаем с параметрами или интерактивно
sudo bash install.sh -d myshop.ru -e admin@myshop.ru
```

### Что такое REACT_APP_BACKEND_URL?

Это **НЕ адрес API 4tochki**!

Это адрес **ВАШЕГО backend сервера** на вашем VPS:
- С доменом: `https://ваш-домен.com`
- Без домена: `http://localhost:8001`

Установщик настраивает это автоматически! ✅

### Можно ли установить SSL потом?

Да! Установите сначала без SSL:
```bash
sudo bash install.sh -d myshop.ru --no-ssl
```

Потом добавьте SSL:
```bash
sudo certbot --nginx -d myshop.ru
```

### Как посмотреть логи?

```bash
# Backend
tail -f /var/log/tyres-backend.out.log

# Frontend  
tail -f /var/log/tyres-frontend.out.log

# Nginx
tail -f /var/log/nginx/error.log
```

### Приложение не работает?

```bash
# Проверьте статус
sudo supervisorctl status

# Перезапустите всё
sudo supervisorctl restart all

# Проверьте что MongoDB работает
sudo systemctl status mongod
```

---

## 📞 Нужна помощь?

- **Полная документация:** [SETUP_GUIDE.md](SETUP_GUIDE.md)
- **API 4tochki:** [b2b.4tochki.ru/Help](https://b2b.4tochki.ru/Help/Page?url=index.html)
- **Telegram Bots:** [core.telegram.org/bots](https://core.telegram.org/bots/api)

---

## ✅ Чеклист установки

- [ ] Сервер с Ubuntu 20.04+
- [ ] Домен указывает на IP сервера
- [ ] Скачал install.sh
- [ ] Запустил с доменом и email
- [ ] Заполнил .env с учетными данными
- [ ] Перезапустил сервисы
- [ ] Проверил статус (все RUNNING)
- [ ] Создал Mini App в BotFather
- [ ] Указал URL приложения
- [ ] Протестировал в Telegram

🎉 **Готово!** Ваш магазин шин работает!
