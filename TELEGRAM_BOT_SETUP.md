# Настройка Telegram бота для команды /start

## 📍 Где находится текст приветствия:

**Файл:** `/opt/tyres-app/telegram_bot.py`

**Строки 34-44:** Приветственное сообщение для пользователей

```python
welcome_text = (
    f"🎉 Добро пожаловать, {user.first_name}!\n\n"
    f"🚗 <b>Интернет-магазин шин и дисков</b>\n\n"
    f"У нас вы найдёте:\n"
    f"✅ Самые выгодные цены на шины и диски\n"
    f"✅ Огромный выбор брендов и моделей\n"
    f"✅ Подбор по автомобилю\n"
    f"✅ Доставка в ваш город\n\n"
    f"Нажмите кнопку <b>\"Магазин\"</b> внизу слева и подберите шины для вашего автомобиля!\n\n"
    f"💰 Наценка минимальная, качество — максимальное!"
)
```

**Строки 57-65:** Уведомление для админа о новом посетителе

```python
admin_notification = (
    f"👋 <b>Новый посетитель!</b>\n\n"
    f"🆔 ID: <code>{user.id}</code>\n"
    # ... остальной текст
)
```

## 🔧 Как редактировать текст приветствия:

```bash
# 1. Откройте файл
sudo nano /opt/tyres-app/telegram_bot.py

# 2. Найдите строки 34-44 (welcome_text)

# 3. Измените текст по своему вкусу
# Можно использовать:
# {user.first_name} - имя пользователя
# <b>текст</b> - жирный текст
# <i>текст</i> - курсив
# <code>текст</code> - моноширинный шрифт
# \n - новая строка
# \n\n - пустая строка

# 4. Сохраните: Ctrl+O, Enter, Ctrl+X

# 5. Перезапустите бота
sudo supervisorctl restart tyres-telegram-bot
```

## 🚀 Как запустить бота (первоначальная настройка):

### Шаг 1: Проверьте переменные окружения

```bash
# Проверьте файл .env
cat /opt/tyres-app/backend/.env | grep -E "TELEGRAM_BOT_TOKEN|ADMIN_TELEGRAM_ID|WEBAPP_URL"

# Должно быть:
# TELEGRAM_BOT_TOKEN=ваш_токен
# ADMIN_TELEGRAM_ID=ваш_id
# WEBAPP_URL=https://ваш_домен
```

### Шаг 2: Установите зависимости для бота

```bash
cd /opt/tyres-app
source backend/venv/bin/activate
pip install python-telegram-bot python-dotenv
```

### Шаг 3: Создайте конфигурацию Supervisor

Проверьте существует ли конфигурация:

```bash
ls /etc/supervisor/conf.d/ | grep tyres
```

Если нет файла с конфигурацией бота, создайте его:

```bash
sudo nano /etc/supervisor/conf.d/tyres-bot.conf
```

Добавьте содержимое:

```ini
[program:tyres-telegram-bot]
command=/opt/tyres-app/backend/venv/bin/python3 telegram_bot.py
directory=/opt/tyres-app
user=root
autostart=true
autorestart=true
stderr_logfile=/var/log/tyres-telegram-bot.err.log
stdout_logfile=/var/log/tyres-telegram-bot.out.log
environment=PATH="/opt/tyres-app/backend/venv/bin"
```

### Шаг 4: Обновите Supervisor и запустите бота

```bash
# Обновите конфигурацию
sudo supervisorctl reread
sudo supervisorctl update

# Запустите бота
sudo supervisorctl start tyres-telegram-bot

# Проверьте статус
sudo supervisorctl status tyres-telegram-bot
```

### Шаг 5: Проверьте логи

```bash
# Если бот не запустился, смотрите логи
sudo tail -f /var/log/tyres-telegram-bot.err.log

# Или
sudo supervisorctl tail tyres-telegram-bot stderr
```

## 🧪 Тестирование:

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Должно прийти приветственное сообщение
4. Админу должно прийти уведомление о новом посетителе

## ❌ Если бот не запускается:

### Ошибка: TELEGRAM_BOT_TOKEN not found

**Решение:**
```bash
# Проверьте что токен есть в .env
sudo nano /opt/tyres-app/backend/.env

# Добавьте строку если отсутствует:
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# Перезапустите
sudo supervisorctl restart tyres-telegram-bot
```

### Ошибка: Module 'telegram' not found

**Решение:**
```bash
cd /opt/tyres-app
source backend/venv/bin/activate
pip install python-telegram-bot
sudo supervisorctl restart tyres-telegram-bot
```

### Ошибка: Conflict: can't use getUpdates

**Причина:** Бот уже запущен в другом месте или через webhook

**Решение:**
```bash
# Остановите все процессы бота
ps aux | grep telegram_bot
sudo kill -9 <PID>

# Или отключите webhook через BotFather
# /setwebhook -> удалите webhook

# Перезапустите
sudo supervisorctl restart tyres-telegram-bot
```

## 📝 Примеры кастомизации текста:

### Минималистичный вариант:
```python
welcome_text = (
    f"Привет, {user.first_name}! 👋\n\n"
    f"Добро пожаловать в магазин шин и дисков!\n\n"
    f"Нажмите кнопку <b>\"Магазин\"</b> внизу, чтобы начать."
)
```

### С промокодом:
```python
welcome_text = (
    f"🎉 Здравствуйте, {user.first_name}!\n\n"
    f"🚗 <b>Магазин автошин</b>\n\n"
    f"✅ Лучшие цены\n"
    f"✅ Доставка по всей России\n"
    f"✅ Гарантия качества\n\n"
    f"🎁 <b>Промокод на первый заказ: FIRST10</b>\n\n"
    f"Откройте магазин через кнопку внизу! 👇"
)
```

### С контактами:
```python
welcome_text = (
    f"Добро пожаловать в наш магазин, {user.first_name}!\n\n"
    f"📞 Телефон: +7 (xxx) xxx-xx-xx\n"
    f"⏰ Работаем: Пн-Пт 9:00-18:00\n\n"
    f"Выберите товары через кнопку <b>\"Магазин\"</b>"
)
```

## 🔄 После изменения текста:

```bash
# Просто перезапустите бота
sudo supervisorctl restart tyres-telegram-bot

# Изменения вступят в силу сразу
```

## ✅ Что делает бот:

1. **Отвечает на /start** - отправляет приветственное сообщение пользователю
2. **Уведомляет админа** - когда новый пользователь пишет /start (кроме самого админа)
3. **Отвечает на /help** - показывает список команд

**Важно:** Бот НЕ обрабатывает заказы и уведомления о заказах - это делает backend напрямую!
