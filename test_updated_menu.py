#!/usr/bin/env python3
"""
Быстрый тест обновленного текста меню Шиномонтаж
"""

import asyncio
import os
from dotenv import load_dotenv
from telegram import Bot

load_dotenv('/app/backend/.env')

BOT_TOKEN = os.environ.get('TELEGRAM_BOT_TOKEN')
ADMIN_ID = os.environ.get('ADMIN_TELEGRAM_ID')

async def test():
    bot = Bot(token=BOT_TOKEN)
    
    test_message = (
        "🧪 <b>Тест обновленного меню</b>\n\n"
        "✅ Текст меню 'Шиномонтаж' обновлен!\n\n"
        "📝 <b>Что добавлено:</b>\n"
        "💡 Дополнительная возможность:\n"
        "Мы можем реализовать функционал автоматической записи на ваш шиномонтаж "
        "с отслеживанием даты и времени клиентов за дополнительную плату.\n\n"
        "🧪 <b>Как проверить:</b>\n"
        "1. Отправьте /start\n"
        "2. Нажмите '🔧 Шиномонтаж'\n"
        "3. Проверьте что текст содержит информацию о доп возможности"
    )
    
    await bot.send_message(
        chat_id=ADMIN_ID,
        text=test_message,
        parse_mode='HTML'
    )
    print("✅ Тестовое сообщение отправлено!")
    print("📱 Проверьте бот: https://t.me/shoptyresbot")
    print("   1. /start")
    print("   2. Нажмите 'Шиномонтаж'")
    print("   3. Проверьте обновленный текст")

if __name__ == '__main__':
    asyncio.run(test())
