# 🔧 Исправление: Запрос учетных данных при установке

## Проблема
При установке учетные данные не запрашивались.

## Решение
Исправлен скрипт установки. Теперь есть 3 способа передать учетные данные:

---

## Способ 1: Интерактивный режим (РЕКОМЕНДУЕТСЯ)

```bash
# Скачайте обновленный скрипт
wget https://raw.githubusercontent.com/wrx861/tyres/main/install.sh

# Запустите
sudo bash install.sh
```

**Установщик последовательно запросит:**
1. Домен (можно пропустить)
2. SSL email (если домен указан)
3. **Логин 4tochki API** ← НОВОЕ!
4. **Пароль 4tochki API** ← НОВОЕ!
5. **Telegram Bot Token** ← НОВОЕ!
6. **Admin Telegram ID** ← НОВОЕ!

> **Подсказка:** Можно нажать Enter чтобы пропустить, данные можно добавить позже

---

## Способ 2: Полностью автоматический режим

Передайте все данные через аргументы командной строки:

```bash
sudo bash install.sh \
  -d tires.yourdomain.com \
  -e admin@yourdomain.com \
  --api-login ваш_логин \
  --api-password ваш_пароль \
  --bot-token 123456:ABC-DEF \
  --admin-id 508352361
```

**Преимущества:**
- ✅ Полностью автоматическая установка
- ✅ Идеально для скриптов и CI/CD
- ✅ Никакого ручного ввода

---

## Способ 3: Ручная настройка (как раньше)

Если вы уже установили приложение:

```bash
# Отредактируйте файл
sudo nano /opt/tyres-app/backend/.env
```

Заполните:
```env
FOURTHCHKI_LOGIN=ваш_логин
FOURTHCHKI_PASSWORD=ваш_пароль
TELEGRAM_BOT_TOKEN=123456:ABC-DEF
ADMIN_TELEGRAM_ID=508352361
```

Перезапустите:
```bash
sudo supervisorctl restart all
```

---

## Где взять учетные данные?

### 1. API 4tochki
- Сайт: https://b2b.4tochki.ru
- Регистрация → Получите логин и пароль
- Документация: https://b2b.4tochki.ru/Help/Page?url=index.html

### 2. Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (формат: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 3. Admin Telegram ID
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Бот пришлет ваш ID (число, например: `508352361`)

---

## Проверка что данные сохранены

После установки проверьте файл:
```bash
cat /opt/tyres-app/backend/.env
```

Должно быть:
```env
MONGO_URL=mongodb://localhost:27017/tyres_db
FOURTHCHKI_LOGIN=sa56026
FOURTHCHKI_PASSWORD=********
FOURTHCHKI_API_URL=http://api-b2b.4tochki.ru/WCF/ClientService.svc?wsdl
TELEGRAM_BOT_TOKEN=123456:ABC-DEF
ADMIN_TELEGRAM_ID=508352361
DEFAULT_MARKUP_PERCENTAGE=15
USE_MOCK_DATA=false
```

Если данные не заполнены (`your_login_here`), используйте **Способ 3** выше.

---

## Тестирование установщика

Для проверки что запросы работают, можно запустить тестовый скрипт:

```bash
bash /app/test_install_prompts.sh
```

Он просто запросит данные и покажет что получено, без установки.

---

## Помощь

Просмотр всех опций установщика:
```bash
sudo bash install.sh --help
```

Вывод покажет все доступные параметры и примеры использования.
