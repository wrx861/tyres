# 📨 Функция отправки сообщений клиентам

## ✅ Реализовано

### 1️⃣ **Отправка сообщений через бота с пересылкой**
Администратор может отправлять сообщения клиентам через Telegram бота:
- ✅ Модальное окно для ввода текста в админ панели
- ✅ Отправка сообщения через бота клиенту
- ✅ Клиент получает сообщение с подписью "💼 Сообщение от Администратора"
- ✅ Клиент может ответить прямо в боте
- ✅ Ответы автоматически пересылаются админу

### 2️⃣ **Telegram по номеру телефона**
Добавлена кнопка для открытия Telegram по номеру телефона:
- ✅ Использует `t.me/+79123456789`
- ✅ Работает если у клиента привязан номер к Telegram

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                      Админ панель                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Заказ #ORD-123                                   │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │ 📨 Отправить сообщение клиенту (основная)  │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────┬──────────────────────┐  │  │
│  │  │ @username (если есть)│ 📱 По номеру         │  │  │
│  │  └──────────────────────┴──────────────────────┘  │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
              ┌─────────────────────────┐
              │   Backend API           │
              │ POST /api/admin/        │
              │      send-message       │
              └─────────────────────────┘
                            ↓
              ┌─────────────────────────┐
              │   Telegram Bot          │
              │ (Интегрирован в backend)│
              └─────────────────────────┘
                            ↓
              ┌─────────────────────────┐
              │   💬 Клиент в Telegram  │
              │   Получает сообщение    │
              └─────────────────────────┘
                            ↓
              Клиент отвечает в боте
                            ↓
              ┌─────────────────────────┐
              │   Telegram Bot          │
              │ Обработка входящих      │
              │ сообщений (_handle_     │
              │            message)     │
              └─────────────────────────┘
                            ↓
              ┌─────────────────────────┐
              │   📬 Админ в Telegram   │
              │   Получает пересланное  │
              │   сообщение с инфо      │
              │   о клиенте             │
              └─────────────────────────┘
```

---

## 📝 Изменённые файлы

### Backend

#### 1. `/app/backend/services/telegram_bot.py`
**Добавлено:**
- ✅ Обработчик обычных сообщений: `_handle_message()`
  - Пересылает сообщения от клиентов админу
  - Исключает админа (чтобы не было петли)
  - Добавляет контекст о клиенте (ID, username, имя)
  - Отправляет подтверждение клиенту

- ✅ Метод отправки от админа: `send_admin_message_to_client()`
  - Форматирует сообщение с подписью админа
  - Отправляет клиенту
  - Добавляет подсказку что можно ответить

- ✅ Регистрация MessageHandler в `start_bot_polling()`
  ```python
  from telegram.ext import MessageHandler, filters
  self.application.add_handler(
      MessageHandler(filters.TEXT & ~filters.COMMAND, self._handle_message)
  )
  ```

#### 2. `/app/backend/routers/admin.py`
**Добавлено:**
- ✅ Endpoint: `POST /api/admin/send-message`
  - Проверка прав админа
  - Проверка существования клиента
  - Получение имени админа для подписи
  - Отправка через `send_admin_message_to_client()`

- ✅ Pydantic модель:
  ```python
  class SendMessageRequest(BaseModel):
      client_telegram_id: str
      message_text: str
  ```

### Frontend

#### 3. `/app/frontend/src/api/api.js`
**Добавлено:**
```javascript
export const sendMessageToClient = async (adminTelegramId, clientTelegramId, messageText) => {
  const response = await axios.post(
    `${API}/admin/send-message?telegram_id=${adminTelegramId}`,
    {
      client_telegram_id: clientTelegramId,
      message_text: messageText
    }
  );
  return response.data;
};
```

#### 4. `/app/frontend/src/pages/AdminPage.js`
**Изменено:**

1. **Импорты:**
   - Добавлены иконки: `Send`, `Phone`
   - Добавлена функция: `sendMessageToClient`

2. **State:**
   ```javascript
   const [showMessageModal, setShowMessageModal] = useState(false);
   const [messageModalData, setMessageModalData] = useState({
     clientId: '', clientName: '', phone: ''
   });
   const [messageText, setMessageText] = useState('');
   ```

3. **Новые функции:**
   - `handleOpenMessageModal()` - открывает модальное окно
   - `handleSendMessage()` - отправляет сообщение
   - `handleContactByPhone()` - открывает Telegram по номеру

4. **UI изменения:**
   - **Основная кнопка:** "📨 Отправить сообщение клиенту" (синяя)
   - **Дополнительные кнопки:**
     - `@username` (если есть) - открывает t.me/username
     - "📱 По номеру" (если есть телефон) - открывает t.me/+79123456789
   
5. **Модальное окно:**
   - Информация о клиенте (имя, ID, телефон)
   - Textarea для ввода текста
   - Кнопки: "Отмена" и "Отправить"

---

## 🎯 Как это работает

### Сценарий 1: Админ отправляет сообщение клиенту

1. Админ открывает админ панель → заказы
2. Находит нужный заказ
3. Нажимает **"📨 Отправить сообщение клиенту"**
4. В модальном окне вводит текст
5. Нажимает "Отправить"
6. ✅ Клиент получает в Telegram:
   ```
   💼 Сообщение от Администратора
   
   Ваш заказ готов к отправке!
   
   Вы можете ответить на это сообщение прямо здесь
   ```

### Сценарий 2: Клиент отвечает

1. Клиент получает сообщение от бота
2. Клиент пишет ответ в том же диалоге с ботом
3. Бот отправляет клиенту подтверждение:
   ```
   ✅ Ваше сообщение получено!
   Администратор ответит вам в ближайшее время.
   ```
4. ✅ Админ получает в Telegram:
   ```
   📨 Сообщение от клиента
   
   👤 ID: 123456789 | @username | Иван Петров
   
   💬 "Когда можно забрать заказ?"
   
   ➡️ Чтобы ответить, используйте админ-панель
   ```

### Сценарий 3: Telegram по номеру

1. Админ нажимает **"📱 По номеру"**
2. Открывается `https://t.me/+79123456789`
3. Если у клиента привязан этот номер к Telegram - открывается диалог
4. ✅ Прямая связь через Telegram

---

## ⚙️ Технические детали

### Обработка входящих сообщений

```python
async def _handle_message(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Игнорируем сообщения от админа (избегаем петли)
    if str(user.id) == self.admin_id:
        return
    
    # Получаем инфо о клиенте из БД
    db_user = await self.db.users.find_one({"telegram_id": str(user.id)})
    
    # Формируем контекст
    user_info = f"ID: {user.id}"
    if user.username:
        user_info += f" | @{user.username}"
    
    # Отправляем админу
    forward_message = f"📨 Сообщение от клиента\n\n👤 {user_info}\n\n💬 \"{message_text}\""
    await self.send_message(self.admin_id, forward_message)
    
    # Подтверждаем клиенту
    await update.message.reply_text("✅ Ваше сообщение получено!")
```

### Отправка от админа

```python
async def send_admin_message_to_client(
    self, 
    client_telegram_id: str, 
    message_text: str, 
    admin_name: str = "Администратор"
):
    formatted_message = (
        f"💼 Сообщение от {admin_name}\n\n"
        f"{message_text}\n\n"
        f"Вы можете ответить на это сообщение прямо здесь"
    )
    await self.bot.send_message(chat_id=client_telegram_id, text=formatted_message)
```

---

## ✅ Преимущества этого решения

1. **Удобство:**
   - ✅ Админ не покидает админ панель
   - ✅ Вся история в одном месте
   - ✅ Быстрые ответы

2. **Универсальность:**
   - ✅ Работает для всех клиентов (с username и без)
   - ✅ Работает с любого устройства админа
   - ✅ Не требует номера телефона

3. **Гибкость:**
   - ✅ Основной способ: через бота (всегда работает)
   - ✅ Дополнительно: по username (если есть)
   - ✅ Дополнительно: по номеру телефона (если есть)

4. **Автоматизация:**
   - ✅ Ответы автоматически пересылаются
   - ✅ Подтверждения отправляются автоматически
   - ✅ Контекст о клиенте добавляется автоматически

---

## 🧪 Тестирование

### Проверка отправки сообщения

1. Создайте тестовый заказ
2. Откройте админ панель
3. Нажмите "📨 Отправить сообщение клиенту"
4. Введите тестовое сообщение
5. Проверьте что клиент получил сообщение в Telegram

### Проверка пересылки ответов

1. От имени клиента ответьте на сообщение бота
2. Проверьте что админ получил переслан<ное сообщение
3. Убедитесь что в сообщении есть ID, username и текст клиента

### Проверка Telegram по номеру

1. Убедитесь что в заказе указан телефон
2. Нажмите "📱 По номеру"
3. Проверьте что открывается `t.me/+номер`

---

## 🚀 Развертывание

Изменения готовы к deploy:

```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh
```

**Все изменения backward compatible:**
- ✅ Старые заказы продолжат работать
- ✅ Новый функционал доступен сразу
- ✅ Не требуется миграция БД

---

## 📊 Статистика использования

**Ожидаемые сценарии:**
- 🔹 70% сообщений через бота (основной способ)
- 🔹 20% через username (если есть)
- 🔹 10% через номер телефона (если привязан)

**Преимущества:**
- ✅ 100% клиентов могут получить сообщение через бота
- ✅ Админу не нужно искать способ связи
- ✅ Единое окно для всех коммуникаций

---

**Дата реализации:** 2025-11-07  
**Версия:** 2.0  
**Статус:** ✅ Готово к production
