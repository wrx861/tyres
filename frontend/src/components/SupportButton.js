import React from 'react';
import { MessageCircle } from 'lucide-react';

const SupportButton = () => {
  const handleSupportClick = () => {
    // Показываем инструкцию как связаться с поддержкой
    if (window.Telegram && window.Telegram.WebApp) {
      window.Telegram.WebApp.showAlert(
        '💬 Поддержка\n\n' +
        'Для связи с администратором:\n\n' +
        '1️⃣ Напишите сообщение прямо в этого бота\n\n' +
        '2️⃣ Ваше сообщение будет автоматически переслано администратору\n\n' +
        '3️⃣ Администратор ответит вам через бота в течение нескольких минут\n\n' +
        '✉️ Просто напишите боту и получите помощь!',
        () => {
          // После закрытия alert - закрываем Mini App чтобы пользователь мог написать боту
          window.Telegram.WebApp.close();
        }
      );
    } else {
      // Fallback для браузера
      alert(
        'Для связи с поддержкой:\n\n' +
        '1. Напишите сообщение боту в Telegram\n' +
        '2. Ваше сообщение будет переслано администратору\n' +
        '3. Администратор ответит вам через бота'
      );
    }
  };

  return (
    <button
      onClick={handleSupportClick}
      className="fixed bottom-4 right-4 bg-blue-500 hover:bg-blue-600 text-white px-4 py-3 rounded-full shadow-lg flex items-center space-x-2 transition-all hover:scale-105 z-50"
      aria-label="Поддержка"
      title="Связаться с поддержкой"
    >
      <MessageCircle size={20} />
      <span className="font-medium">Поддержка</span>
    </button>
  );
};

export default SupportButton;
