# Добавление кнопки "Написать" в разделе Пользователи

## Обзор изменений

Добавлена возможность отправки сообщений клиентам через бота прямо из раздела "Пользователи" в админ-панели.

## Проблема

Ранее админ мог отправить сообщение клиенту через бота только в разделе "Заказы". 

Если клиент нажимал кнопку "Поддержка" и задавал вопрос, у админа не было возможности ответить ему из раздела "Пользователи" - приходилось искать его заказ или переходить в Telegram напрямую.

## Решение

### 1. Добавлена кнопка "Написать"

В разделе "Пользователи" теперь у каждого пользователя есть кнопка **"Написать"** с иконкой сообщения.

### 2. Модальное окно для отправки сообщений

При нажатии на кнопку открывается то же модальное окно, что используется в разделе заказов:
- Отображается имя клиента
- Поле для ввода текста сообщения
- Кнопка "Отправить"

### 3. Отправка через бота

Сообщение отправляется клиенту через Telegram бота с подписью "От администратора:".

## UI Изменения

### До:
```
┌─────────────────────────────────┐
│ Иван Иванов                     │
│ @ivan                           │
│ ID: 123456789                   │
│                                 │
│ [    Заблокировать    ]         │ <- Большая кнопка на всю ширину
└─────────────────────────────────┘
```

### После:
```
┌─────────────────────────────────┐
│ Иван Иванов                     │
│ @ivan                           │
│ ID: 123456789                   │
│                                 │
│ [💬 Написать] [✕]               │ <- "Написать" + маленькая кнопка блокировки
└─────────────────────────────────┘
```

## Технические детали

### Файл: `/app/frontend/src/pages/AdminPage.js`

#### 1. Добавлена новая функция:
```javascript
const handleOpenMessageModalForUser = (user) => {
  setMessageModalData({
    clientId: user.telegram_id,
    clientName: `${user.first_name || ''} ${user.last_name || ''}`.trim() || `User ${user.telegram_id}`,
    phone: ''
  });
  setMessageText('');
  setShowMessageModal(true);
};
```

#### 2. Обновлен UI раздела пользователей:
```javascript
<div className="flex gap-2">
  {/* Кнопка написать */}
  <button
    onClick={() => handleOpenMessageModalForUser(u)}
    className="flex-1 bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-lg text-sm font-medium transition-colors flex items-center justify-center space-x-2"
  >
    <MessageCircle size={16} />
    <span>Написать</span>
  </button>
  
  {/* Кнопка блокировки (компактная) */}
  {!u.is_admin && (
    <button
      onClick={() => u.is_blocked ? handleUnblockUser(u.telegram_id) : handleBlockUser(u.telegram_id)}
      className={`py-2 px-3 rounded-lg text-sm font-medium transition-colors ${
        u.is_blocked
          ? 'bg-green-500 hover:bg-green-600 text-white'
          : 'bg-red-500 hover:bg-red-600 text-white'
      }`}
    >
      {u.is_blocked ? '✓' : '✕'}
    </button>
  )}
</div>
```

## Преимущества

✅ **Быстрый ответ на обращения** - админ может сразу написать клиенту из раздела пользователей

✅ **Единый интерфейс** - используется то же модальное окно, что и в заказах

✅ **Компактный UI** - кнопка блокировки теперь занимает меньше места (иконка ✓/✕)

✅ **Удобство** - не нужно искать заказ клиента чтобы ему написать

## Использование

1. Админ открывает раздел **"Пользователи"** в админ-панели
2. Находит нужного пользователя в списке
3. Нажимает кнопку **"Написать"** (синяя с иконкой 💬)
4. Вводит текст сообщения в модальном окне
5. Нажимает **"Отправить"**
6. Сообщение приходит клиенту в Telegram через бота

## Backend

Изменений в backend не требуется - используется существующий endpoint:
- `POST /api/admin/send-message`

Функция из `api.js`:
- `sendMessageToClient(adminId, clientId, messageText)`

---
**Дата:** 8 ноября 2025  
**Версия:** 1.0
