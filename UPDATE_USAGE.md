# 📋 Использование скрипта обновления update.sh

## 🎯 Три режима работы:

### 1. Интерактивный режим (рекомендуется)
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh
```

**Что произойдет:**
- Скрипт **спросит**: "Создать backup перед обновлением? (y/n)"
- Подождет 15 секунд для ответа
- `y` или `Y` - создаст backup
- `n` или `N` или Enter или таймаут - пропустит backup
- Продолжит обновление

### 2. Быстрый режим без backup
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh --no-backup
```

**Что произойдет:**
- Backup **не создается** совсем
- НЕ спрашивает
- Быстрое обновление

### 3. Автоматический режим с backup
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh -y
```

**Что произойдет:**
- Backup создается **автоматически**
- НЕ спрашивает
- Полезно для автоматизации/cron

## 📊 Сравнение режимов:

| Режим | Команда | Backup | Спрашивает | Скорость |
|-------|---------|--------|------------|----------|
| Интерактивный | `update.sh` | По выбору | ✅ Да | Средняя |
| Без backup | `update.sh --no-backup` | ❌ Нет | ❌ Нет | Быстро |
| Автоматический | `update.sh -y` | ✅ Да | ❌ Нет | Средняя |

## 🎬 Примеры использования:

### Первое обновление (безопасно):
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh
# Ответить: y (создать backup)
```

### Срочное обновление:
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh --no-backup
```

### Регулярное обновление (каждый день):
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh
# Ответить: n (не нужен backup каждый день)
```

### Перед важным изменением:
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh -y
# Автоматически создаст backup
```

## ⚙️ Автоматизация через cron:

### Вариант 1: Без backup (быстрее)
```bash
# Добавить в crontab (sudo crontab -e):
0 3 * * * cd /opt/tyres-app && git pull origin main && bash update.sh --no-backup >> /var/log/tyres-update.log 2>&1
```

### Вариант 2: С backup (безопаснее)
```bash
# Добавить в crontab (sudo crontab -e):
0 3 * * * cd /opt/tyres-app && git pull origin main && bash update.sh -y >> /var/log/tyres-update.log 2>&1
```

## 🔍 Управление backup:

### Посмотреть все backup:
```bash
ls -lah /opt | grep tyres-app-backup
```

### Удалить старые вручную:
```bash
# Оставить только последние 3
ls -dt /opt/tyres-app-backup-* | tail -n +4 | xargs rm -rf
```

### Удалить все backup:
```bash
rm -rf /opt/tyres-app-backup-*
```

### Восстановить из backup:
```bash
# Остановить сервисы
sudo supervisorctl stop all

# Восстановить
sudo rm -rf /opt/tyres-app
sudo cp -r /opt/tyres-app-backup-20251107_143000 /opt/tyres-app

# Запустить
sudo supervisorctl start all
```

## 💡 Советы:

1. **При первом обновлении** - всегда создавайте backup (`y`)
2. **При частых обновлениях** - backup не обязателен (`n` или `--no-backup`)
3. **Скрипт автоматически** оставляет только 3 последних backup
4. **15 секунд таймаут** - если не ответите, backup не создастся
5. **Backup занимает ~200MB** - учитывайте место на диске

## ❓ FAQ:

**Q: Что если я случайно нажал не ту кнопку?**
A: Нажмите Ctrl+C и запустите скрипт снова.

**Q: Могу ли я изменить таймаут ожидания?**
A: Да, в строке `read -t 15` измените 15 на нужное количество секунд.

**Q: Сколько места занимают backup?**
A: Каждый backup ~200MB. 3 backup = ~600MB.

**Q: Можно ли автоматически создавать backup раз в неделю?**
A: Да, используйте cron с флагом `-y`:
```bash
# Каждое воскресенье в 3 утра с backup
0 3 * * 0 cd /opt/tyres-app && git pull origin main && bash update.sh -y
```

---

**Рекомендация:** Для повседневных обновлений используйте интерактивный режим - скрипт спросит и вы решите нужен ли backup.
