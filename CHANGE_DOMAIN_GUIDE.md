# 🌐 Автоматическая смена домена и SSL сертификата

## ✅ Что делает скрипт `change-domain.sh`

Автоматически меняет домен приложения и получает SSL сертификат за **~2-3 минуты**.

### Основные функции:
1. ✅ Проверка DNS и портов
2. ✅ Создание backup конфигураций
3. ✅ Обновление nginx конфигурации
4. ✅ Автоматическое получение SSL сертификата (Let's Encrypt)
5. ✅ Обновление frontend .env (REACT_APP_BACKEND_URL)
6. ✅ Пересборка frontend с новым URL
7. ✅ Перезапуск всех сервисов
8. ✅ Проверка работоспособности
9. ✅ Настройка автообновления SSL
10. ✅ Откат при ошибках

---

## 🚀 Быстрый старт

### 1. Смена домена с SSL (рекомендуется)
```bash
cd /opt/tyres-app
sudo bash change-domain.sh -d tires.newdomain.com -e admin@example.com
```

### 2. Смена домена без SSL
```bash
cd /opt/tyres-app
sudo bash change-domain.sh -d tires.newdomain.com --no-ssl
```

### 3. Принудительная смена (без проверок DNS)
```bash
cd /opt/tyres-app
sudo bash change-domain.sh -d tires.newdomain.com -e admin@example.com --force
```

---

## 📋 Подробное использование

### Синтаксис
```bash
sudo bash change-domain.sh [ОПЦИИ]
```

### Опции

| Опция | Описание | Обязательно |
|-------|----------|-------------|
| `-d, --domain DOMAIN` | Новый домен | ✅ Да |
| `-e, --email EMAIL` | Email для Let's Encrypt | ✅ Да (для SSL) |
| `--no-ssl` | Не получать SSL сертификат | ❌ Нет |
| `-f, --force` | Пропустить проверки DNS/портов | ❌ Нет |
| `-h, --help` | Показать справку | ❌ Нет |

---

## 🔍 Что происходит пошагово

### [1/11] Проверка DNS
Проверяет что домен указывает на IP сервера:
```
DNS запись: tires.newdomain.com → 203.0.113.1
IP сервера: 203.0.113.1
✓ DNS настроен корректно
```

**Можно пропустить:** используйте `--force`

### [2/11] Создание backup
Создает резервные копии:
- `/opt/tyres-app-domain-backup-20250107_150530/`
  - `nginx-tyres` - конфигурация nginx
  - `frontend.env` - frontend .env
  - `backend.env` - backend .env

**Хранится:** последние 3 backup

### [3/11] Обновление nginx
Создает новую конфигурацию nginx с:
- `server_name` = новый домен
- Proxy для frontend (порт 3000)
- Proxy для backend API (порт 8001)

### [4/11] Перезапуск nginx
```bash
nginx -t  # Проверка конфигурации
systemctl reload nginx
```

### [5/11] Получение SSL сертификата
Если указан `-e` (email):
```bash
certbot --nginx -d tires.newdomain.com \
  --email admin@example.com \
  --agree-tos --non-interactive --redirect
```

**Результат:**
- SSL сертификат получен
- nginx автоматически настроен на HTTPS
- HTTP → HTTPS редирект

### [6/11] Обновление frontend/.env
```bash
# Было:
REACT_APP_BACKEND_URL=https://old-domain.com

# Стало:
REACT_APP_BACKEND_URL=https://tires.newdomain.com
```

### [7/11] Пересборка frontend
```bash
cd /opt/tyres-app/frontend
yarn build
```

**Время:** ~1-2 минуты

### [8/11] Перезапуск сервисов
```bash
supervisorctl restart frontend
supervisorctl restart backend
```

### [9/11] Проверка работоспособности
Проверяет:
- ✅ Nginx работает
- ✅ Backend API работает (http://localhost:8001/api/health)
- ✅ Frontend работает (http://localhost:3000)
- ✅ Домен доступен через HTTPS

### [10/11] Настройка автообновления SSL
```bash
systemctl enable certbot.timer
systemctl start certbot.timer
```

**Проверка автообновления:**
```bash
systemctl list-timers | grep certbot
```

### [11/11] Очистка старых backup
Оставляет только последние 3 backup, остальные удаляет.

---

## ⚙️ Подготовка к смене домена

### 1. Настройка DNS (за 1-24 часа до смены)

Создайте A-запись в вашем DNS провайдере:

```
Тип:  A
Имя:  tires (или @)
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL:  300 (5 минут) или Auto
```

**Пример для популярных провайдеров:**

**Cloudflare:**
1. Dashboard → Domains → your-domain.com → DNS
2. Add record
   - Type: A
   - Name: tires
   - IPv4 address: ваш IP
   - Proxy status: 🔴 DNS only (не через CDN!)
   - TTL: Auto
3. Save

**Namecheap:**
1. Domain List → Manage → Advanced DNS
2. Add New Record
   - Type: A Record
   - Host: tires
   - Value: ваш IP
   - TTL: 5 min
3. Save

**GoDaddy:**
1. My Products → Domains → DNS
2. Add
   - Type: A
   - Name: tires
   - Value: ваш IP
   - TTL: 600 seconds
3. Save

### 2. Проверка DNS (перед запуском скрипта)

```bash
# Проверка что домен указывает на ваш сервер
dig +short tires.newdomain.com

# Должен показать IP вашего сервера
# Если не показывает - подождите распространения DNS
```

### 3. Открытие портов

Убедитесь что порты открыты:

```bash
# Проверка портов
sudo netstat -tulnp | grep -E ':(80|443)'

# Если закрыты - откройте
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

---

## 📊 Примеры использования

### Пример 1: Стандартная смена с SSL
```bash
# Подготовка
# 1. Создали A-запись: tires.mydomain.com → 203.0.113.1
# 2. Подождали 5-10 минут
# 3. Проверили: dig +short tires.mydomain.com

# Запуск
cd /opt/tyres-app
sudo bash change-domain.sh \
  -d tires.mydomain.com \
  -e admin@mydomain.com

# Результат:
# ✓ DNS проверен
# ✓ Backup создан
# ✓ Nginx обновлен
# ✓ SSL сертификат получен
# ✓ Frontend пересобран
# ✓ Сервисы перезапущены
# 
# 🌐 Приложение доступно: https://tires.mydomain.com
# 🔒 SSL сертификат установлен
```

### Пример 2: Смена без SSL
```bash
cd /opt/tyres-app
sudo bash change-domain.sh \
  -d tires.mydomain.com \
  --no-ssl

# Результат:
# 🌐 Приложение доступно: http://tires.mydomain.com
# ⚠️ SSL не установлен
```

### Пример 3: Принудительная смена (DNS еще не распространился)
```bash
cd /opt/tyres-app
sudo bash change-domain.sh \
  -d tires.mydomain.com \
  -e admin@mydomain.com \
  --force

# Пропускает проверку DNS
# Полезно если домен настроен, но DNS еще не обновился
```

### Пример 4: Откат к старому домену
```bash
# Если что-то пошло не так, откат:
BACKUP_DIR="/opt/tyres-app-domain-backup-20250107_150530"

sudo cp $BACKUP_DIR/frontend.env /opt/tyres-app/frontend/.env
sudo cp $BACKUP_DIR/nginx-tyres /etc/nginx/sites-available/tyres
sudo systemctl reload nginx
sudo supervisorctl restart all

# Старый домен восстановлен
```

---

## 🔍 Проверка после смены

### 1. Проверка nginx
```bash
sudo systemctl status nginx
# Должен быть: active (running)
```

### 2. Проверка backend
```bash
curl https://tires.newdomain.com/api/health
# Ожидаем: {"status":"healthy","database":"connected"}
```

### 3. Проверка frontend
```bash
curl https://tires.newdomain.com
# Ожидаем: HTML код приложения
```

### 4. Проверка SSL
```bash
openssl s_client -connect tires.newdomain.com:443 -servername tires.newdomain.com < /dev/null

# Или через браузер:
# 1. Откройте https://tires.newdomain.com
# 2. Кликните на замок 🔒 в адресной строке
# 3. Проверьте сертификат
```

### 5. Проверка автообновления SSL
```bash
sudo systemctl list-timers | grep certbot

# Должно показать:
# certbot.timer ... left ... certbot.service
```

---

## ❌ Возможные проблемы и решения

### Проблема 1: DNS не найден
```
✗ DNS запись для tires.newdomain.com не найдена
```

**Решение:**
1. Проверьте что A-запись создана
2. Подождите 5-30 минут (распространение DNS)
3. Используйте `--force` для принудительного продолжения

### Проблема 2: Ошибка получения SSL
```
✗ Ошибка получения SSL сертификата
```

**Причины:**
- Порты 80/443 закрыты
- DNS еще не обновился
- Превышен лимит Let's Encrypt (5 сертификатов в неделю на домен)

**Решение:**
```bash
# Проверьте порты
sudo netstat -tulnp | grep -E ':(80|443)'

# Проверьте DNS
dig +short tires.newdomain.com

# Попробуйте без SSL
sudo bash change-domain.sh -d tires.newdomain.com --no-ssl

# Установите SSL позже
sudo certbot --nginx -d tires.newdomain.com
```

### Проблема 3: Frontend не пересобрался
```
✗ Ошибка пересборки frontend
```

**Решение:**
```bash
cd /opt/tyres-app/frontend
yarn install
yarn build

# Если ошибка с памятью:
export NODE_OPTIONS="--max-old-space-size=2048"
yarn build
```

### Проблема 4: Backend не отвечает
```
⚠ Backend API не отвечает
```

**Решение:**
```bash
# Проверьте логи
tail -50 /var/log/tyres-backend.err.log

# Перезапустите
sudo supervisorctl restart backend

# Проверьте статус
curl http://localhost:8001/api/health
```

---

## 🔐 Безопасность

### Let's Encrypt лимиты:
- **5 сертификатов в неделю** на домен
- **50 сертификатов в неделю** на аккаунт
- **5 дубликатов в неделю** (тот же набор доменов)

**Рекомендация:** Тестируйте без SSL (`--no-ssl`), получайте SSL только когда всё работает.

### Автообновление SSL:
Certbot автоматически обновляет сертификаты за 30 дней до истечения.

**Проверка:**
```bash
sudo certbot renew --dry-run
```

### Права доступа:
Скрипт требует root прав для:
- Изменения nginx конфигурации
- Получения SSL сертификата
- Перезапуска сервисов

---

## 📊 Таймлайн смены домена

```
T-24h:  Создать A-запись DNS
        ↓
T-1h:   Проверить DNS (dig +short)
        ↓
T-0:    Запустить change-domain.sh
        ↓
+2min:  Получен SSL, пересобран frontend
        ↓
+3min:  Сервисы перезапущены
        ↓
+5min:  Проверка работоспособности
        ↓
DONE:   Новый домен работает с SSL!
```

---

## 🎯 Что входит в backup

При каждой смене создается backup:
```
/opt/tyres-app-domain-backup-20250107_150530/
├── nginx-tyres         # Конфигурация nginx
├── frontend.env        # Frontend переменные
└── backend.env         # Backend переменные
```

**Хранится:** 3 последних backup  
**Размер:** ~10KB каждый  

---

## ✅ Чек-лист смены домена

### Перед сменой:
- [ ] Создана A-запись DNS
- [ ] DNS распространился (проверено через dig)
- [ ] Порты 80 и 443 открыты
- [ ] Есть email для Let's Encrypt
- [ ] Приложение работает на старом домене

### После смены:
- [ ] Nginx работает (systemctl status nginx)
- [ ] Backend отвечает (curl /api/health)
- [ ] Frontend загружается
- [ ] SSL сертификат валиден (проверка в браузере)
- [ ] Автообновление SSL настроено
- [ ] Telegram бот обновлен (если есть webhook на домен)

---

## 🚀 Интеграция в CI/CD

Скрипт можно использовать в автоматизации:

```bash
#!/bin/bash
# deploy-to-production.sh

# Получаем параметры из переменных окружения
NEW_DOMAIN=${PRODUCTION_DOMAIN}
ADMIN_EMAIL=${ADMIN_EMAIL}

# Смена домена
sudo bash change-domain.sh \
  -d $NEW_DOMAIN \
  -e $ADMIN_EMAIL

# Проверка
if curl -s https://$NEW_DOMAIN/api/health | grep -q "healthy"; then
  echo "✓ Production deployment successful"
  exit 0
else
  echo "✗ Production deployment failed"
  exit 1
fi
```

---

## 📞 Поддержка

### Логи для диагностики:
```bash
# Nginx
tail -f /var/log/nginx/error.log

# Backend
tail -f /var/log/tyres-backend.err.log

# Frontend
tail -f /var/log/tyres-frontend.err.log

# Certbot
tail -f /var/log/letsencrypt/letsencrypt.log
```

### Полезные команды:
```bash
# Статус всех сервисов
sudo supervisorctl status

# Перезапуск всех сервисов
sudo supervisorctl restart all

# Проверка nginx конфигурации
sudo nginx -t

# Просмотр списка сертификатов
sudo certbot certificates

# Ручное обновление SSL
sudo certbot renew
```

---

**Создано:** 2025-11-07  
**Версия:** 1.0  
**Статус:** ✅ Готов к использованию
