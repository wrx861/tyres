# ✅ Добавлен выбор брендов в Frontend

## 🎯 Что обновлено:

### 1. API клиент (`frontend/src/api/api.js`)
Добавлены функции для получения списка брендов:
```javascript
export const getTireBrands = async () => {
  const response = await axios.get(`${API}/products/brands/tires`);
  return response.data;
};

export const getDiskBrands = async () => {
  const response = await axios.get(`${API}/products/brands/disks`);
  return response.data;
};
```

### 2. Страница поиска (`frontend/src/pages/SearchPage.js`)

#### Добавлены состояния:
```javascript
const [tireBrands, setTireBrands] = useState([]);
const [diskBrands, setDiskBrands] = useState([]);
const [brandsLoading, setBrandsLoading] = useState(false);
```

#### Добавлена загрузка брендов при монтировании:
```javascript
useEffect(() => {
  const loadBrands = async () => {
    setBrandsLoading(true);
    try {
      const [tiresResponse, disksResponse] = await Promise.all([
        getTireBrands(),
        getDiskBrands()
      ]);
      setTireBrands(tiresResponse.brands || []);
      setDiskBrands(disksResponse.brands || []);
    } catch (error) {
      console.error('Ошибка загрузки брендов:', error);
    } finally {
      setBrandsLoading(false);
    }
  };
  loadBrands();
}, []);
```

#### Заменено текстовое поле на dropdown:

**Было:**
```javascript
<input
  type="text"
  value={filters.brand}
  onChange={(e) => setFilters({ ...filters, brand: e.target.value })}
  placeholder="Например: Michelin"
/>
```

**Стало:**
```javascript
<select
  value={filters.brand}
  onChange={(e) => setFilters({ ...filters, brand: e.target.value })}
  disabled={brandsLoading}
>
  {brandsLoading ? (
    <option value="">Загрузка...</option>
  ) : (
    <>
      {searchType === 'tires' ? (
        tireBrands.map(brand => (
          <option key={brand} value={brand === 'Любой' ? '' : brand}>
            {brand}
          </option>
        ))
      ) : (
        diskBrands.map(brand => (
          <option key={brand} value={brand === 'Любой' ? '' : brand}>
            {brand}
          </option>
        ))
      )}
    </>
  )}
</select>
```

## 🎨 Как это работает:

### 1. При загрузке страницы:
- Автоматически запрашиваются списки брендов шин и дисков
- Пока загружается - показывается "Загрузка..."
- После загрузки - dropdown заполняется брендами

### 2. При выборе типа товара:
- Если выбраны "Шины" - показываются бренды шин (103 бренда)
- Если выбраны "Диски" - показываются бренды дисков (10 брендов)

### 3. При поиске:
- Выбранный бренд отправляется в параметре `brand`
- Если выбран "Любой" - параметр не передается (пустая строка)
- API фильтрует результаты по выбранному бренду

## 📊 Структура данных:

### Ответ от API `/api/products/brands/tires`:
```json
{
  "success": true,
  "brands": [
    "Любой",
    "Altenzo",
    "Antares",
    ...
    "Yokohama",
    "Кировский Ш3"
  ],
  "total": 103
}
```

### Ответ от API `/api/products/brands/disks`:
```json
{
  "success": true,
  "brands": [
    "Любой",
    "Replay",
    "LegeArtis",
    "СКАД",
    ...
  ],
  "total": 10
}
```

## 🧪 Тестирование:

### Проверка на локальном сервере:
1. Откройте приложение в браузере
2. Перейдите на страницу "Поиск"
3. Проверьте dropdown "Бренд":
   - Должен показывать "Загрузка..." на 1-2 секунды
   - Затем заполниться списком из 103 брендов для шин
   - При переключении на "Диски" - список должен измениться на 10 брендов

### Проверка фильтрации:
1. Выберите бренд "Michelin"
2. Установите диаметр "15"
3. Нажмите "Найти"
4. Результаты должны содержать только шины Michelin R15

## 🔧 Обновление на production:

### Вариант 1: Автоматическое обновление (hot reload)
Frontend имеет hot reload, изменения применяются автоматически.

### Вариант 2: Ручной перезапуск
```bash
cd /opt/tyres-app
git pull origin main
sudo supervisorctl restart tyres-frontend
```

### Вариант 3: Использование update.sh
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh
```

## ✅ Проверка работы:

### В консоли браузера:
```javascript
// Откройте DevTools (F12) -> Console
// Проверьте запрос к API брендов
fetch('https://tyres.vpnsuba.ru/api/products/brands/tires')
  .then(r => r.json())
  .then(console.log);
```

### В Network вкладке DevTools:
При загрузке страницы поиска должны появиться запросы:
- `GET /api/products/brands/tires` → Status 200
- `GET /api/products/brands/disks` → Status 200

## 📝 Измененные файлы:

- `frontend/src/api/api.js` - добавлены функции `getTireBrands()` и `getDiskBrands()`
- `frontend/src/pages/SearchPage.js` - заменено текстовое поле на dropdown с автозагрузкой

## 💡 Дальнейшие улучшения (опционально):

1. **Поиск по брендам:** Добавить текстовое поле для быстрого поиска бренда в dropdown
2. **Популярные бренды:** Показывать топ-10 популярных брендов отдельно
3. **Избранные бренды:** Позволить пользователю сохранить избранные бренды
4. **Кэширование:** Сохранять список брендов в localStorage для быстрой загрузки

---

**Дата:** 2025-11-07  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
