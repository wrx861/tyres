# 🤝 Contributing Guide

Спасибо за интерес к проекту! Мы рады любому вкладу.

## 🚀 Начало работы

### 1. Fork и клонирование:

```bash
git clone https://github.com/YOUR_USERNAME/tyres.git
cd tyres
```

### 2. Установка для разработки:

**Backend:**
```bash
cd backend
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# Отредактируйте .env
```

**Frontend:**
```bash
cd frontend
yarn install
cp .env.example .env
# Отредактируйте .env
```

### 3. Запуск в dev режиме:

**Backend (с hot reload):**
```bash
cd backend
source venv/bin/activate
uvicorn server:app --reload --host 0.0.0.0 --port 8001
```

**Frontend (с hot reload):**
```bash
cd frontend
yarn start
```

## 📝 Code Style

### Python (Backend):

- Следуем **PEP 8**
- Используем **type hints**
- Документируем функции docstrings

```python
def search_tires(
    width: int,
    height: int,
    diameter: int
) -> List[Dict[str, Any]]:
    """
    Поиск шин по параметрам.
    
    Args:
        width: Ширина шины в мм
        height: Высота профиля
        diameter: Диаметр в дюймах
        
    Returns:
        Список найденных шин
    """
    pass
```

### JavaScript/React (Frontend):

- Используем **ES6+**
- Функциональные компоненты с **hooks**
- **PropTypes** для типизации

```javascript
const ProductCard = ({ product, onAddToCart }) => {
  // Component code
};

ProductCard.propTypes = {
  product: PropTypes.object.isRequired,
  onAddToCart: PropTypes.func.isRequired
};
```

## 🔀 Git Workflow

### Ветки:

- `main` - продакшен-готовый код
- `develop` - разработка
- `feature/название` - новые фичи
- `fix/название` - исправления

### Commit Messages:

```
feat: добавлен фильтр по брендам
fix: исправлена фильтрация по городу
docs: обновлена документация API
style: форматирование кода
refactor: рефакторинг поиска товаров
test: добавлены тесты для orders API
```

### Pull Request:

1. Создайте ветку от `develop`
2. Внесите изменения
3. Напишите тесты (если применимо)
4. Создайте PR в `develop`
5. Опишите изменения подробно

## 🧪 Тестирование

### Backend:

```bash
cd backend
pytest tests/
```

### Frontend:

```bash
cd frontend
yarn test
```

## 📦 Добавление зависимостей

### Backend:

```bash
pip install package_name
pip freeze > requirements.txt
```

### Frontend:

```bash
yarn add package-name
```

## 🐛 Отчеты об ошибках

При создании Issue укажите:

- **Описание** проблемы
- **Шаги** для воспроизведения
- **Ожидаемое** поведение
- **Фактическое** поведение
- **Логи** (если есть)
- **Окружение** (ОС, версия Python/Node.js)

## 💡 Предложения фичей

- Опишите проблему, которую решает фича
- Предложите решение
- Приведите примеры использования

## 📖 Документация

При добавлении нового API эндпоинта:

1. Добавьте docstring в функцию
2. Обновите README.md (раздел API)
3. Swagger документация обновится автоматически

## ✅ Checklist перед PR:

- [ ] Код соответствует стандартам проекта
- [ ] Добавлены/обновлены тесты
- [ ] Обновлена документация
- [ ] Все тесты проходят
- [ ] Нет конфликтов с `develop`
- [ ] Commit messages понятны

## 🎯 Приоритетные задачи:

1. **Тесты** - покрытие тестами < 50%
2. **Документация API** - примеры запросов
3. **Обработка ошибок** - улучшение error handling
4. **Производительность** - кеширование запросов к API
5. **UI/UX** - улучшение мобильной версии

## 📞 Контакты

- **Issues**: [GitHub Issues](https://github.com/wrx861/tyres/issues)
- **Discussions**: [GitHub Discussions](https://github.com/wrx861/tyres/discussions)

## 📜 Лицензия

Внося вклад, вы соглашаетесь с лицензией MIT проекта.

---

Спасибо за вклад! 🎉
