# 🚀 Быстрая инструкция по обновлению

## На вашем production сервере выполните:

```bash
# 1. Перейдите в директорию приложения
cd /opt/tyres-app

# 2. Получите последние изменения
git pull origin main

# 3. Запустите скрипт обновления
sudo bash update.sh
```

## Или если хотите только frontend:

```bash
cd /opt/tyres-app
git pull origin main
sudo supervisorctl restart tyres-frontend

# Подождите 10-15 секунд пока frontend соберется и запустится
sleep 15

# Проверьте статус
sudo supervisorctl status tyres-frontend
```

## Что изменилось:

✅ **Frontend:** Dropdown для выбора бренда вместо текстового поля
✅ **Backend:** Endpoints для получения списка брендов (уже работают)

## После обновления:

1. Откройте приложение в Telegram
2. Перейдите на страницу "Поиск"
3. Найдите поле "Бренд"
4. Теперь это должен быть **dropdown** со списком брендов

## Если не видите изменений:

```bash
# Очистите кэш браузера или
# Сделайте Hard Reload в Telegram WebApp

# В крайнем случае - пересборка frontend:
cd /opt/tyres-app/frontend
yarn build
sudo supervisorctl restart tyres-frontend
```

---

**Важно:** Изменения уже в GitHub, просто нужно сделать `git pull` на сервере!
