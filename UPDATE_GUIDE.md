# 🔄 Руководство по обновлению приложения

## 📋 Быстрое обновление (рекомендуется)

### На production сервере выполните:

```bash
# 1. Загрузите скрипт обновления с GitHub
cd /opt/tyres-app
git pull origin main

# 2. Запустите обновление
sudo bash update.sh
```

Скрипт автоматически:
- ✅ Создаст backup текущей версии
- ✅ Получит последние изменения из GitHub
- ✅ Обновит зависимости (если изменились)
- ✅ Удалит старые процессы (tyres-telegram-bot)
- ✅ Перезапустит сервисы
- ✅ Проверит работоспособность
- ✅ Предложит откат если что-то пошло не так

## 🎯 Что делает скрипт update.sh

### Этап 1: Backup
Создает полную копию приложения в `/opt/tyres-app-backup-YYYYMMDD_HHMMSS`

### Этап 2-3: Git Pull
Получает и применяет последние изменения из GitHub

### Этап 4: Зависимости
Автоматически определяет изменения в:
- `backend/requirements.txt` → обновляет Python пакеты
- `frontend/package.json` → обновляет Node.js пакеты

### Этап 5: Конфигурация
Удаляет устаревшие конфигурации (например, старый tyres-telegram-bot)

### Этап 6: Перезапуск
Корректно перезапускает backend (определяет название процесса автоматически)

### Этап 7-8: Проверка
Проверяет:
- Backend API доступность
- Telegram бот запущен
- Новые endpoints работают
- Статус всех сервисов

## 🔧 Ручное обновление (если нужно)

Если хотите контролировать процесс вручную:

```bash
cd /opt/tyres-app

# 1. Backup
sudo cp -r /opt/tyres-app /opt/tyres-app-backup-$(date +%Y%m%d_%H%M%S)

# 2. Git pull
git fetch origin
git pull origin main

# 3. Обновить зависимости (если нужно)
/opt/tyres-app/backend/venv/bin/pip install -r /opt/tyres-app/backend/requirements.txt

# 4. Удалить старый процесс telegram-bot (если есть)
sudo supervisorctl stop tyres-telegram-bot
sudo rm -f /etc/supervisor/conf.d/tyres-telegram-bot.conf
sudo supervisorctl reread
sudo supervisorctl update

# 5. Перезапустить backend
sudo supervisorctl restart tyres-backend

# 6. Проверить статус
sudo supervisorctl status
```

## ⚠️ Откат к предыдущей версии

Если обновление вызвало проблемы:

```bash
# 1. Остановить сервисы
sudo supervisorctl stop all

# 2. Восстановить из backup
sudo rm -rf /opt/tyres-app
sudo cp -r /opt/tyres-app-backup-YYYYMMDD_HHMMSS /opt/tyres-app

# 3. Запустить снова
sudo supervisorctl start all

# 4. Проверить
sudo supervisorctl status
```

## 📊 Проверка после обновления

### 1. Проверка сервисов
```bash
sudo supervisorctl status
```
Должны быть:
- `tyres-backend RUNNING`
- `tyres-frontend RUNNING` или `STOPPED` (если не используется)
- **НЕ должно быть** `tyres-telegram-bot`

### 2. Проверка логов
```bash
# Последние 30 строк логов backend
tail -30 /var/log/tyres-backend.err.log

# Проверка запуска Telegram бота
tail -50 /var/log/tyres-backend.err.log | grep telegram

# Мониторинг в реальном времени
tail -f /var/log/tyres-backend.err.log
```

### 3. Проверка API
```bash
# Health check
curl http://localhost:8001/api/health

# Новые endpoints брендов
curl http://localhost:8001/api/products/brands/tires | python3 -m json.tool | head -20
```

### 4. Проверка Telegram бота
1. Откройте бот: @shoptyresbot
2. Отправьте `/start`
3. Проверьте:
   - Текст "Доставка на ваш адрес"
   - Кнопка "🔧 Шиномонтаж"
   - Меню шиномонтажа с информацией о доп. возможности
   - Прайс-лист
   - Контакт @malg1nov

## 🚀 Автоматизация (опционально)

### Создать cron задачу для регулярных обновлений

```bash
# Редактировать crontab
sudo crontab -e

# Добавить строку (обновление каждый день в 3 ночи)
0 3 * * * cd /opt/tyres-app && git pull origin main && bash update.sh >> /var/log/tyres-update.log 2>&1
```

### Webhook для автоматического обновления при push в GitHub

Создать endpoint в FastAPI:
```python
@app.post("/webhook/update")
async def github_webhook(request: Request):
    # Проверка secret
    # Запуск update.sh
    subprocess.run(["bash", "/opt/tyres-app/update.sh"])
    return {"status": "updated"}
```

## 💡 Лучшие практики

### Перед обновлением
1. ✅ Проверьте что backup создан
2. ✅ Убедитесь что есть свободное место на диске
3. ✅ Сообщите пользователям о возможном downtime
4. ✅ Сохраните важные логи

### После обновления
1. ✅ Проверьте все основные функции
2. ✅ Мониторьте логи первые 10-15 минут
3. ✅ Проверьте Telegram бота
4. ✅ Тестируйте новые функции

### Если проблемы
1. ✅ Проверьте логи: `/var/log/tyres-backend.err.log`
2. ✅ Проверьте статус: `sudo supervisorctl status`
3. ✅ Откатитесь к backup если критично
4. ✅ Свяжитесь с поддержкой с логами

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи
2. Попробуйте откат
3. Обратитесь к разработчикам с описанием проблемы и логами

---

**Автор:** Emergent AI  
**Дата создания:** 2025-11-07  
**Версия:** 1.0
