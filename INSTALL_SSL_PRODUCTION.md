# 🔒 Установка SSL на production сервере

## 📊 Текущее состояние

Проверка показала:
```
✅ Backend работает
✅ Frontend работает
✅ Все процессы запущены
✅ MongoDB работает
❌ SSL не установлен
❌ Backend URL: http:// (должен быть https://)
```

---

## ✅ Установка SSL (2 минуты)

### Шаг 1: Запустить скрипт установки SSL

```bash
cd /opt/tyres-app
sudo bash change-domain.sh -d tyres.shopmarketbot.ru -e wrx861@yandex.ru
```

**Что произойдёт:**
1. Удалит старые сертификаты (если есть)
2. Получит новый SSL сертификат (Let's Encrypt)
3. Установит в nginx
4. Обновит frontend/.env на https://
5. Пересоберёт frontend
6. Перезапустит все сервисы

**Время:** ~2-3 минуты

---

### Шаг 2: Проверка после установки

```bash
# 1. Проверить что SSL работает
curl -I https://tyres.shopmarketbot.ru

# Ожидаем: HTTP/2 200

# 2. Проверить frontend .env
cat /opt/tyres-app/frontend/.env | grep REACT_APP_BACKEND_URL

# Должно быть: REACT_APP_BACKEND_URL=https://tyres.shopmarketbot.ru

# 3. Проверить статус
sudo supervisorctl status

# Всё должно быть: RUNNING
```

---

## 🔍 Проверка портов (если беспокоит)

### Правильная команда для проверки портов:

```bash
# Вместо netstat используйте ss
sudo ss -tulnp | grep -E ":(80|443|8001|3000)"

# Должно показать:
# *:80    ... nginx
# *:443   ... nginx (после установки SSL)
# *:8001  ... python/uvicorn
# *:3000  ... node/serve
```

**Или:**

```bash
# Через lsof
sudo lsof -i :80
sudo lsof -i :443
sudo lsof -i :8001
sudo lsof -i :3000
```

**Почему netstat может не показывать:**
- netstat может быть не установлен
- Процессы слушают на 127.0.0.1 вместо 0.0.0.0
- Но если API отвечает - значит порты работают!

---

## 📝 Пошаговая установка SSL

### 1. Проверка DNS (перед установкой)

```bash
dig +short tyres.shopmarketbot.ru

# Должен показать IP вашего сервера
# Если нет - подождите 5-30 минут
```

### 2. Запуск установки SSL

```bash
cd /opt/tyres-app

sudo bash change-domain.sh \
  -d tyres.shopmarketbot.ru \
  -e wrx861@yandex.ru
```

**Ожидаемый вывод:**
```
[1/11] Проверка DNS...
✓ DNS настроен корректно

[2/11] Создание backup...
✓ Backup создан

[3/11] Подготовка nginx...
✓ Nginx готов

[4/11] Конфигурация nginx будет создана на шаге SSL

[5/11] Получение и установка SSL сертификата...
→ Шаг 1/3: Получение SSL сертификата...
✓ SSL сертификат успешно получен

→ Шаг 2/3: Установка сертификата в nginx...
✓ Nginx конфигурация с SSL валидна

→ Шаг 3/3: Перезагрузка nginx...
✓ SSL сертификат успешно установлен
✓ HTTPS работает корректно

[6/11] Обновление frontend/.env...
✓ Frontend .env обновлен
Новый REACT_APP_BACKEND_URL: https://tyres.shopmarketbot.ru

[7/11] Пересборка frontend...
✓ Frontend пересобран

[8/11] Перезапуск сервисов...
✓ Frontend перезапущен
✓ Backend перезапущен

[9/11] Проверка работоспособности...
✓ Nginx работает
✓ Backend API работает
✓ Frontend работает
✓ Домен доступен через HTTPS

[10/11] Настройка автообновления SSL...
✓ Автообновление SSL настроено

[11/11] Очистка старых backup...
✓ Старых backup нет

================================
  Смена домена завершена!
================================

🌐 Приложение доступно: https://tyres.shopmarketbot.ru
📱 Backend API: https://tyres.shopmarketbot.ru/api
🔒 SSL: Установлен и будет обновляться автоматически
```

### 3. Финальная проверка

```bash
# SSL сертификат
openssl s_client -connect tyres.shopmarketbot.ru:443 -servername tyres.shopmarketbot.ru < /dev/null | grep "Verify return code"

# Должно быть: Verify return code: 0 (ok)

# Backend API через HTTPS
curl https://tyres.shopmarketbot.ru/api/health

# Должно быть: {"status":"healthy","database":"connected"}

# Frontend через HTTPS
curl -I https://tyres.shopmarketbot.ru

# Должно быть: HTTP/2 200
```

---

## 🎯 После установки SSL

### Обновить URL в BotFather

1. Откройте @BotFather в Telegram
2. `/mybots`
3. Выберите вашего бота
4. Bot Settings → Menu Button → Edit Menu Button URL
5. Введите: `https://tyres.shopmarketbot.ru` (с https!)
6. Сохраните

### Проверить Mini App

1. Откройте бота в Telegram
2. Закройте Telegram полностью
3. Откройте снова
4. Нажмите кнопку меню
5. Mini App должен загрузиться
6. Должно показывать ваше имя (не "Гость")

---

## 🔧 Если что-то пошло не так

### Ошибка: DNS не найден

```bash
# Проверьте DNS
dig +short tyres.shopmarketbot.ru

# Если пусто - подождите или используйте --force
sudo bash change-domain.sh -d tyres.shopmarketbot.ru -e wrx861@yandex.ru --force
```

### Ошибка: Port 80 already in use

```bash
# Проверьте что nginx остановлен
sudo systemctl stop nginx

# Или найдите что использует порт
sudo lsof -i :80

# Убейте процесс
sudo kill -9 PID

# Затем запустите скрипт снова
```

### Ошибка: Too many certificates

```bash
# Лимит Let's Encrypt: 5 сертификатов в неделю
# Проверьте сколько сертификатов уже получено
curl -s "https://crt.sh/?q=shopmarketbot.ru&output=json" | jq length

# Если превышен лимит - подождите неделю
# Или используйте другой домен
```

---

## 📊 Результат после установки SSL

### ✅ Проверка check-installation.sh

```bash
sudo bash /opt/tyres-app/check-installation.sh

# Должно показать:
# [8/10] Проверка портов...
# ✓ Nginx порт 80 слушается
# ✓ Nginx порт 443 (SSL) слушается

# Backend URL: https://tyres.shopmarketbot.ru  ← https!
```

### ✅ В браузере

```
https://tyres.shopmarketbot.ru
```

- Замок 🔒 в адресной строке
- Сертификат от Let's Encrypt
- Действителен ~90 дней
- Автообновление настроено

### ✅ В Telegram Mini App

- Показывает ваше имя
- Можно добавлять товары
- Можно оформлять заказы
- Админ панель доступна

---

## 🚀 Быстрая команда (всё в одну строку)

```bash
cd /opt/tyres-app && sudo bash change-domain.sh -d tyres.shopmarketbot.ru -e wrx861@yandex.ru && sudo bash check-installation.sh
```

Установит SSL и сразу проверит что всё работает!

---

## 💡 Полезная информация

### Автообновление SSL

Certbot автоматически обновит сертификат за 30 дней до истечения:

```bash
# Проверка автообновления
sudo systemctl list-timers | grep certbot

# Ручное обновление (если нужно)
sudo certbot renew

# Тест обновления (dry-run)
sudo certbot renew --dry-run
```

### Просмотр всех сертификатов

```bash
sudo certbot certificates

# Покажет:
# Certificate Name: tyres.shopmarketbot.ru
# Domains: tyres.shopmarketbot.ru
# Expiry Date: 2026-02-06
# Certificate Path: /etc/letsencrypt/live/...
```

### Удаление сертификата (если нужно)

```bash
sudo certbot delete --cert-name tyres.shopmarketbot.ru
```

---

**Дата:** 2025-11-08  
**Домен:** tyres.shopmarketbot.ru  
**Статус:** ✅ Готов к установке SSL
