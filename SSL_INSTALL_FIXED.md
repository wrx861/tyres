# 🔧 Исправленный скрипт установки SSL

## ✅ Что исправлено

### Проблема:
```
✓ SSL сертификат получен
✗ Certbot не смог установить в nginx
⚠️ Работает без SSL (HTTP)
```

### Решение:

Новая логика установки SSL в 3 шага:

**1. Удаление старых сертификатов** (если есть)
```bash
certbot delete --cert-name DOMAIN
```

**2. Получение сертификата (standalone режим)**
```bash
# Останавливаем nginx
systemctl stop nginx

# Получаем сертификат
certbot certonly --standalone -d DOMAIN

# Запускаем nginx
systemctl start nginx
```

**3. Ручная установка в nginx**
```nginx
server {
    listen 443 ssl http2;
    server_name DOMAIN;
    
    ssl_certificate /etc/letsencrypt/live/DOMAIN/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/DOMAIN/privkey.pem;
    ...
}
```

---

## 🚀 Использование обновлённого скрипта

### На production сервере:

```bash
# 1. Получить обновлённый скрипт
cd /opt/tyres-app
git pull origin main

# 2. Запустить с SSL
sudo bash change-domain.sh \
  -d tyres.shopmarketbot.ru \
  -e wrx861@yandex.ru

# Скрипт автоматически:
# ✓ Удалит старый сертификат (если есть)
# ✓ Получит новый сертификат (standalone)
# ✓ Создаст nginx конфигурацию с SSL
# ✓ Настроит HTTP → HTTPS редирект
# ✓ Перезапустит все сервисы
```

---

## 📋 Что происходит пошагово

### [1/11] Проверка DNS
```
DNS запись: tyres.shopmarketbot.ru → IP сервера
✓ DNS настроен корректно
```

### [2/11] Backup
```
✓ Backup создан: /opt/tyres-app-domain-backup-TIMESTAMP/
```

### [3/11] Подготовка nginx
```
✓ Определен конфиг: /etc/nginx/sites-available/tyres-app
✓ Symlink создан
```

### [4/11] (Пропущен - конфигурация создается на шаге SSL)

### [5/11] SSL сертификат (3 подшага)

**Подшаг 1/3: Удаление старого (если есть)**
```bash
⚠ Найден существующий сертификат
→ Удаляем старый сертификат...
✓ Старый сертификат удален
```

**Подшаг 2/3: Получение нового**
```bash
→ Шаг 1/3: Получение SSL сертификата...
# Останавливает nginx
# Запускает certbot в standalone режиме
# Запускает nginx обратно
✓ SSL сертификат успешно получен
```

**Подшаг 3/3: Установка в nginx**
```bash
→ Шаг 2/3: Установка сертификата в nginx...
# Создает конфигурацию с SSL
# Проверяет nginx -t
✓ Nginx конфигурация с SSL валидна

→ Шаг 3/3: Перезагрузка nginx...
✓ SSL сертификат успешно установлен
✓ HTTPS работает корректно
```

### [6/11] Обновление frontend .env
```
REACT_APP_BACKEND_URL=https://tyres.shopmarketbot.ru
✓ Frontend .env обновлен
```

### [7/11] Пересборка frontend
```
✓ Frontend пересобран (с новым URL)
```

### [8/11] Перезапуск сервисов
```
✓ Frontend перезапущен
✓ Backend перезапущен
```

### [9/11] Проверка
```
✓ Nginx работает
✓ Backend API работает
✓ Frontend работает
✓ Домен доступен через HTTPS
```

### [10/11] Автообновление SSL
```
✓ Certbot timer активен (автообновление каждые 60 дней)
```

### [11/11] Очистка backup
```
✓ Оставлены последние 3 backup
```

---

## 🎯 Результат

После успешного выполнения:

```
================================
  Смена домена завершена!
================================

Старый домен: localhost
Новый домен: tyres.shopmarketbot.ru

🌐 Приложение доступно: https://tyres.shopmarketbot.ru
📱 Backend API: https://tyres.shopmarketbot.ru/api
🔒 SSL: Установлен и будет обновляться автоматически

Backup конфигураций: /opt/tyres-app-domain-backup-TIMESTAMP

🎉 Домен успешно изменен!
```

---

## ✅ Проверка SSL

### 1. Через curl
```bash
curl -I https://tyres.shopmarketbot.ru

# Ожидаем: HTTP/2 200
```

### 2. Проверка сертификата
```bash
openssl s_client -connect tyres.shopmarketbot.ru:443 \
  -servername tyres.shopmarketbot.ru < /dev/null | grep -A2 "Verify return code"

# Ожидаем: Verify return code: 0 (ok)
```

### 3. Через браузер
1. Откройте https://tyres.shopmarketbot.ru
2. Кликните на замок 🔒
3. Проверьте сертификат:
   - Выдан: Let's Encrypt
   - Действителен до: ~90 дней
   - Статус: Действителен

### 4. Проверка автообновления
```bash
sudo systemctl list-timers | grep certbot

# Должно показать активный timer
```

---

## 🔧 Если что-то пошло не так

### Проблема 1: Порт 80 занят

**Ошибка:**
```
Problem binding to port 80: Could not bind to IPv4 or IPv6
```

**Решение:**
```bash
# Проверьте что nginx остановлен
sudo systemctl status nginx

# Убедитесь что нет других процессов на порту 80
sudo netstat -tulnp | grep :80

# Если есть - остановите их
sudo systemctl stop apache2  # Если установлен
```

### Проблема 2: DNS не обновился

**Ошибка:**
```
DNS запись для tyres.shopmarketbot.ru не найдена
```

**Решение:**
```bash
# Проверьте DNS
dig +short tyres.shopmarketbot.ru

# Если пусто - подождите 5-30 минут

# Или используйте --force для пропуска проверки
sudo bash change-domain.sh \
  -d tyres.shopmarketbot.ru \
  -e wrx861@yandex.ru \
  --force
```

### Проблема 3: Лимит Let's Encrypt

**Ошибка:**
```
too many certificates already issued
```

**Решение:**
- Лимит: 5 сертификатов в неделю на домен
- Подождите неделю или используйте другой домен
- Проверьте: https://crt.sh/?q=shopmarketbot.ru

### Проблема 4: Nginx ошибка конфигурации

**Ошибка:**
```
nginx: [emerg] unknown directive "ssl_certificate"
```

**Решение:**
```bash
# Проверьте что nginx скомпилирован с SSL
nginx -V 2>&1 | grep -o with-http_ssl_module

# Если нет - переустановите nginx
sudo apt-get install --reinstall nginx
```

---

## 🔄 Откат к HTTP (если нужно)

Если SSL не работает и нужно быстро вернуться на HTTP:

```bash
DOMAIN="tyres.shopmarketbot.ru"

# 1. Создать HTTP конфигурацию
sudo bash -c "cat > /etc/nginx/sites-available/tyres-app << 'EOF'
server {
    listen 80;
    server_name $DOMAIN;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }
    
    location /api {
        proxy_pass http://localhost:8001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }
}
EOF"

# 2. Обновить frontend .env
sudo sed -i "s|REACT_APP_BACKEND_URL=.*|REACT_APP_BACKEND_URL=http://$DOMAIN|g" /opt/tyres-app/frontend/.env

# 3. Пересобрать и перезапустить
cd /opt/tyres-app/frontend && yarn build
sudo nginx -t && sudo systemctl reload nginx
sudo supervisorctl restart all
```

---

## 📊 Преимущества нового подхода

### Старая логика (проблемная):
```
certbot --nginx → ❌ Не может найти server_name
                → ❌ Сертификат получен, но не установлен
```

### Новая логика (надёжная):
```
1. certbot certonly --standalone → ✅ Получает сертификат
2. Создаем nginx конфиг вручную → ✅ Полный контроль
3. nginx reload                  → ✅ SSL работает
```

---

## 💡 Полезные команды

### Просмотр всех сертификатов
```bash
sudo certbot certificates
```

### Ручное обновление сертификата
```bash
sudo certbot renew
```

### Тест обновления (dry-run)
```bash
sudo certbot renew --dry-run
```

### Удаление сертификата
```bash
sudo certbot delete --cert-name tyres.shopmarketbot.ru
```

### Проверка nginx конфигурации
```bash
sudo nginx -t
```

### Просмотр логов certbot
```bash
tail -f /var/log/letsencrypt/letsencrypt.log
```

---

**Дата обновления:** 2025-11-07  
**Версия:** 2.0  
**Статус:** ✅ Исправлено и готово к использованию
