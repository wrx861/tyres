# ✅ Удалено поле "Квартира" из формы доставки

## 🎯 Что изменено:

### Удалено поле "Квартира"

**Причина:** Доставка осуществляется только до дома, не до квартиры.

**Что удалено:**
- ❌ Поле ввода "Квартира" из формы заказа
- ❌ Показ квартиры в админ панели
- ❌ Поле `apartment` из state компонента

**Что осталось:**
- ✅ Город *
- ✅ Улица *
- ✅ Дом *
- ✅ Телефон *
- ✅ Комментарий (опционально)

## 📋 Новая форма доставки:

```
┌─────────────────────────────────────┐
│ Адрес доставки                      │
├─────────────────────────────────────┤
│ Город *          [Тюмень        ]   │
│ Улица *          [Ленина        ]   │
│ Дом *            [15            ]   │
│ Телефон *        [+7 999 123 45 67] │
│ Комментарий      [______________ ]  │
│                  [______________ ]  │
│                  [______________ ]  │
└─────────────────────────────────────┘
```

**В админ панели:**
```
Адрес доставки:
Тюмень, Ленина, д. 15
📞 Телефон: +7 (999) 123-45-67
Комментарий: Позвонить за 30 минут
```

## 📁 Измененные файлы:

### Frontend:
- `frontend/src/pages/CartPage.js`:
  - Удалено поле `apartment` из state
  - Удален input для квартиры из формы
  - Теперь только одно поле "Дом" вместо grid с двумя полями

- `frontend/src/pages/AdminPage.js`:
  - Удален показ квартиры из адреса доставки
  - Адрес теперь: "Город, Улица, д. Дом"

### Backend:
- `backend/models/order.py`:
  - Поле `apartment` остается в модели как `Optional[str]` для обратной совместимости
  - Старые заказы с квартирами остаются в БД

## 🧪 Проверка:

### 1. Создание заказа:
```bash
# Откройте приложение → Корзина → Оформить заказ
# Проверьте что нет поля "Квартира"
# Заполните: Город, Улица, Дом, Телефон
# Создайте заказ
```

### 2. Админ панель:
```bash
# Откройте админ панель → Заказы
# Найдите свежий заказ
# Проверьте адрес: "Город, Улица, д. Дом" (без квартиры)
# Проверьте что показан телефон
```

## 💡 Обратная совместимость:

**Старые заказы с квартирами:**
- Остаются в базе данных
- Квартира НЕ отображается в админке
- При необходимости можно восстановить показ для старых заказов

**Новые заказы:**
- Создаются без поля `apartment`
- Поле остается `null` или пустым в БД

## 🔧 Обновление на production:

```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh
```

Или быстрое обновление:
```bash
cd /opt/tyres-app
git pull origin main
sudo bash update.sh --no-backup
```

## 📝 Примечания:

1. **Hot reload**: Frontend обновится автоматически при запущенном dev server
2. **Backend**: Изменения в модели совместимы со старыми данными
3. **Форма**: Теперь более компактная и понятная для пользователей

---

**Дата:** 2025-11-07  
**Статус:** ✅ Готово
