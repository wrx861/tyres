# 📤 Как загрузить исправления в GitHub

## ✅ Все исправления уже сохранены локально

Все изменения автоматически закоммичены:
- ✅ Исправленный `install.sh` с DB_NAME и CORS_ORIGINS
- ✅ Запрос учетных данных при установке
- ✅ Расширенный поиск дисков (PCD, ET, DIA, цвет, тип)
- ✅ Отображение изображений товаров
- ✅ Уведомления админа о новых посетителях
- ✅ Вся документация (SETUP_GUIDE.md, INSTALL_QUICK.md и др.)

---

## 🔄 Автоматическая синхронизация (Emergent платформа)

Если вы работаете на платформе Emergent:

1. **Нажмите "Save to GitHub"** в интерфейсе Emergent
2. Все изменения автоматически загрузятся в ваш репозиторий
3. После синхронизации установщик будет доступен по адресу:
   ```
   https://raw.githubusercontent.com/wrx861/tyres/main/install.sh
   ```

---

## 💻 Ручная загрузка через Git

Если у вас есть доступ к репозиторию:

```bash
# 1. Клонируйте репозиторий (если ещё не клонирован)
git clone https://github.com/wrx861/tyres.git
cd tyres

# 2. Скопируйте исправленные файлы из /app
cp /app/install.sh ./
cp /app/fix_env.sh ./
cp /app/SETUP_GUIDE.md ./
cp /app/INSTALL_QUICK.md ./
cp /app/FIX_INSTALLATION.md ./
cp /app/CREDENTIALS_FIX.md ./

# 3. Закоммитьте изменения
git add .
git commit -m "Fix: добавлены DB_NAME и CORS_ORIGINS в install.sh, исправлен запрос учетных данных"

# 4. Загрузите в GitHub
git push origin main
```

---

## 🧪 Проверка что изменения загружены

После загрузки проверьте что установщик доступен:

```bash
curl -I https://raw.githubusercontent.com/wrx861/tyres/main/install.sh
# Должно быть: HTTP/2 200
```

Проверьте содержимое:
```bash
curl https://raw.githubusercontent.com/wrx861/tyres/main/install.sh | grep "DB_NAME"
# Должно найти: DB_NAME=tires_shop
```

---

## 📋 Список всех исправленных файлов

Основные файлы, которые нужно загрузить:

### Критичные (обязательно):
- ✅ `install.sh` - исправленный установщик
- ✅ `backend/routers/products.py` - расширенный поиск дисков
- ✅ `backend/services/fourthchki_client.py` - новые параметры API
- ✅ `backend/services/telegram_bot.py` - уведомления о посетителях
- ✅ `backend/routers/auth.py` - отправка уведомлений
- ✅ `frontend/src/components/ProductCard.js` - отображение изображений
- ✅ `frontend/src/components/ProductImageModal.js` - модальное окно (новый)
- ✅ `frontend/src/pages/CartPage.js` - изображения в корзине

### Документация:
- ✅ `SETUP_GUIDE.md` - руководство по установке
- ✅ `INSTALL_QUICK.md` - быстрая установка
- ✅ `FIX_INSTALLATION.md` - исправление проблем
- ✅ `CREDENTIALS_FIX.md` - настройка учетных данных
- ✅ `README.md` - обновлённое описание

### Вспомогательные:
- ✅ `fix_env.sh` - скрипт для исправления .env
- ✅ `test_install_prompts.sh` - тестирование запросов

---

## ✅ После загрузки

Пользователи смогут использовать исправленный установщик:

```bash
wget https://raw.githubusercontent.com/wrx861/tyres/main/install.sh
sudo bash install.sh
```

И он будет:
- ✅ Запрашивать все учетные данные
- ✅ Создавать правильный .env с DB_NAME
- ✅ Работать без ошибок

---

## 🔧 Проверка локальных изменений

Посмотреть что изменилось:
```bash
cd /app
git log --oneline -5
git show HEAD
```

Проверить конкретный файл:
```bash
grep -A5 "DB_NAME" /app/install.sh
```

---

## 📞 Важно

После загрузки в GitHub обязательно протестируйте установщик на чистом сервере, чтобы убедиться что всё работает как ожидается.
