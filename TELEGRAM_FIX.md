# 🔧 Исправление Telegram интеграции

## ✅ Что исправлено:

### 1. **Telegram бот с командой /start**
- Создан полноценный бот (`telegram_bot.py`)
- Отправляет приветствие с описанием магазина
- Кнопка "🛒 Открыть магазин" для запуска Mini App
- Автоматическое уведомление админа о новых посетителях

### 2. **Правильное получение данных пользователя**
- Добавлен Telegram Web App SDK в index.html
- Создана утилита `utils/telegram.js` для работы с SDK
- App.js теперь правильно инициализирует и получает данные пользователя
- Больше НЕ будет показывать "Test" - будет реальное имя/username

### 3. **Проверка количества товара**
- При добавлении в корзину проверяется доступное количество
- Если запрашивается больше чем есть → показывается ошибка
- Уведомление: "⚠️ Доступно только X шт"

---

## 📤 Инструкция по развертыванию:

### Шаг 1: Сохраните в GitHub

**Нажмите "Save to GitHub"**

### Шаг 2: Удалите старую установку

```bash
sudo supervisorctl stop all
sudo rm -rf /opt/tyres-app
```

### Шаг 3: Запустите установку

```bash
wget https://raw.githubusercontent.com/wrx861/tyres/main/install.sh

sudo bash install.sh \
  -d tyres.vpnsuba.ru \
  -e ваш@email.com \
  --api-login sa56026 \
  --api-password F8Aeg3Cnkq \
  --bot-token 8290483601:AAFqXwpx1_paoekO0dt3kR46z471zlDKChI \
  --admin-id 508352361
```

---

## 🤖 Что делает бот:

### Команда /start:

Пользователь отправляет `/start` → получает сообщение:

```
🎉 Добро пожаловать, Иван!

🚗 Интернет-магазин шин и дисков

У нас вы найдёте:
✅ Самые выгодные цены на шины и диски
✅ Огромный выбор брендов и моделей
✅ Подбор по автомобилю
✅ Доставка в ваш город

Нажмите кнопку "Магазин" внизу слева и подберите шины для вашего автомобиля!

💰 Наценка минимальная, качество — максимальное!
```

**Важно:** Кнопка "Магазин" это Menu Button, который настроен в @BotFather

### Уведомление админу:

Когда новый пользователь нажимает `/start`, админ получает:

```
👋 Новый посетитель!

🆔 ID: 123456789
👤 Username: @ivan_user
📝 Имя: Иван Петров
```

---

## 🔍 Проверка после установки:

### 1. Проверьте статус сервисов:

```bash
sudo supervisorctl status
```

Должно быть:
```
tyres-backend         RUNNING ✅
tyres-telegram-bot    RUNNING ✅ (НОВОЕ!)
tyres-frontend        STOPPED ✅
```

### 2. Проверьте логи бота:

```bash
tail -f /var/log/tyres-telegram-bot.out.log
```

Должно быть:
```
INFO:__main__:Bot started successfully!
```

### 3. Проверьте в Telegram:

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Должно прийти приветственное сообщение с кнопкой
4. Нажмите кнопку "🛒 Открыть магазин"
5. Mini App должен открыться

### 4. Проверьте Mini App:

1. Откройте Mini App
2. Должно быть: "Привет, [ваше реальное имя]" (НЕ "Test")
3. Если ваш ID = 508352361 → должна быть кнопка "Админ-панель"
4. Откройте консоль браузера (F12) и посмотрите логи:
   ```
   Telegram Web App initialized: { version: "7.0", platform: "android", user: {...} }
   Telegram user data: { telegram_id: "508352361", username: "...", ... }
   User authenticated: { telegram_id: "508352361", is_admin: true, ... }
   ```

---

## 🎯 Решение проблем:

### Проблема: Бот не отвечает на /start

```bash
# Проверьте логи
tail -f /var/log/tyres-telegram-bot.err.log

# Проверьте токен в .env
grep TELEGRAM_BOT_TOKEN /opt/tyres-app/backend/.env

# Перезапустите бота
sudo supervisorctl restart tyres-telegram-bot
```

### Проблема: Mini App показывает "Test"

Проверьте в консоли браузера (F12):

1. Должен быть лог: `Telegram Web App initialized`
2. Должен быть лог: `Telegram user data: {...}`

Если логов нет:
- Проверьте что скрипт `telegram-web-app.js` загрузился
- Проверьте что приложение открыто ЧЕРЕЗ TELEGRAM (не в обычном браузере)

### Проблема: Нет кнопки "Админ-панель"

```bash
# Проверьте Admin ID в .env
grep ADMIN_TELEGRAM_ID /opt/tyres-app/backend/.env
# Должно быть: ADMIN_TELEGRAM_ID=508352361

# Узнайте ваш реальный ID
# Откройте @userinfobot в Telegram

# Если ID не совпадает, исправьте:
sudo nano /opt/tyres-app/backend/.env
# Измените ADMIN_TELEGRAM_ID=ваш_реальный_id

# Перезапустите backend
sudo supervisorctl restart tyres-backend
```

### Проблема: Не приходят уведомления о посетителях

```bash
# Проверьте логи бота
tail -f /var/log/tyres-telegram-bot.out.log

# При нажатии /start должно быть:
# INFO:__main__:User 123456789 (@username) started the bot

# Проверьте что ADMIN_TELEGRAM_ID настроен
grep ADMIN_TELEGRAM_ID /opt/tyres-app/backend/.env
```

---

## 📱 Настройка бота в @BotFather:

### 1. Настройка Menu Button (кнопка "Магазин"):

1. Откройте @BotFather
2. Отправьте `/mybots`
3. Выберите вашего бота
4. **Bot Settings** → **Menu Button**
5. **Configure Menu Button:**
   - Button text: `Магазин` (или `🛒 Магазин`)
   - URL: `https://tyres.vpnsuba.ru`
6. Готово! Теперь в боте слева внизу будет кнопка "Магазин"

### 2. Настройка команд:

1. В @BotFather выберите бота
2. **Edit Bot** → **Edit Commands**
3. Добавьте:
   ```
   start - Открыть магазин шин и дисков
   help - Показать справку
   ```

---

## ✅ Чеклист:

- [ ] Сохранено в GitHub
- [ ] Удалена старая установка
- [ ] Запущена новая установка
- [ ] tyres-telegram-bot статус RUNNING
- [ ] Бот отвечает на /start
- [ ] Mini App открывается по кнопке
- [ ] Mini App показывает реальное имя (не "Test")
- [ ] Админ получает уведомления о посетителях
- [ ] Кнопка "Админ-панель" видна (если ID совпадает)
- [ ] Проверка количества при добавлении в корзину работает

---

## 🎉 Готово!

После выполнения всех шагов:

✅ Бот отвечает на /start с красивым приветствием  
✅ Mini App получает реальные данные пользователя  
✅ Админ получает уведомления о новых посетителях  
✅ Админ-панель работает для правильного ID  
✅ Проверка количества товара при добавлении  

**Всё работает!** 🚀
